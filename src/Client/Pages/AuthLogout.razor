@page "/logout/{*RedirectUri}"

<Logout RedirectUri="@RedirectUri" />

@code
{
    /// <summary>
    /// Адрес перенаправления после аутентификации.
    /// </summary>
    [Parameter]
    public string? RedirectUri { get; init; }
}